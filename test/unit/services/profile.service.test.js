const profileService = require('../../../src/services/profile.service');

describe('Testing profile service', () => {
  describe('.create', () => {
    it.todo('registers a profile if it does not exists');

    it.todo('does not register two profiles with the same username');
  });
});
